server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  base-url: http://localhost

logging:
  level:
    org.springframework.security: DEBUG